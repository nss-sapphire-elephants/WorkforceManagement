@model BangazonWorkforceSapphireElephants.Models.ViewModels.EmployeeEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Modify Employee Information</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Employee.FirstName" class="control-label">First Name</label>
                <input asp-for="Employee.FirstName" class="form-control" />
                @*<span asp-validation-for="Employee.FirstName" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <label asp-for="Employee.LastName" class="control-label">Last Name</label>
                <input asp-for="Employee.LastName" class="form-control" />
                @*<span asp-validation-for="Employee.LastName" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <label asp-for="Employee.department.Id" class="control-label">Department</label>
                <select asp-for="Employee.department.Id" asp-items="@Model.DepartmentOptions"></select>
                @*<span asp-validation-for="Employee.DepartmentId" class="text-danger"></span>*@
            </div>
            @*
            <div class="form-group">
                <label asp-for="Employee.computer.Id" class="control-label"></label>
                <select asp-for="Employee.computer.Id" asp-items="@Model.ComputerOptions"></select>
                <span asp-validation-for="Employee.computer.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Employee.trainingProgram.Id" class="control-label"></label>
                <select asp-for="Employee.trainingProgram.Id" asp-items="@Model.AddTrainingOption"></select>
                <span asp-validation-for="Employee.trainingProgram.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Employee.trainingProgram.Id" class="control-label"></label>
                <select multiple asp-for="Employee.trainingProgram.Id" asp-items="@Model.RemoveTrainingOption"></select>
                <span asp-validation-for="Employee.trainingProgram.Id" class="text-danger"></span>
            </div>
            *@
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
